-   name: File test
    hosts: localhost
    tasks:
    -   name: Create/update a file
        file:
            name: '/tmp/foo'
            state: merged
            content: |
                Nothing would be what it is because everything would be what it isn't.
                And contrariwise, what it is, it wouldn't be, and what it wouldn't be,
                it would. You see?
        register: r1
    -   name: print result
        debug:
            msg: '{{ r1 }}'

